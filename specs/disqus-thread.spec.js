/** @jsx React.DOM */
var React = require('react/addons');
var TestUtils = React.addons.TestUtils;
var DisqusThread = require('../index');

describe('react-disqus-thread', function () {
  it('should have tests', function () {
    expect(true).toEqual(false);
  });
});